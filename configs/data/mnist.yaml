_target_: src.data.mnist_datamodule.MNISTDataModule
data_dir: ${paths.data_dir}/ludb/data
batch_size: 32 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
n_ludb_train: 100
num_workers: 4
pin_memory: False
